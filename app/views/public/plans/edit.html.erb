<div class="container mx-auto">
  <div class="row">
    <div class="col-sm-12 col-md-10 col-lg-8 bg-white my-3 p-4 shadow-lg mx-auto">
      <%= render "layouts/errors", object: @plan %>
      <%= form_with model: @plan, url: plan_path(@plan), method: :patch do |f| %>
        <h4>プラン概要</h4>
        <div class="row">
          <div class="col">
            <%= f.text_field :title, class: "form-control my-3", data: { plan_id: @plan.id }, placeholder: "プランタイトル" %>
            <%= f.text_area :body, class: "form-control my-3", placeholder: "プラン説明" %>
          </div>
        </div>

        <h4>スケジュール</h4>
        <%= f.fields_for :plan_details do |plan_detail| %>
          <div class="row nested-fields">
            <div class="col-10">
              <%= plan_detail.text_field :title, class: "form-control my-3", data: { plan_detail_id: plan_detail.object.id }, placeholder: "詳細タイトル" %>
            </div>
            <div class="col-2 d-flex align-items-center">
              <%= link_to "#", class: "remove_field btn btn-danger my-3" do %>
                <i class="fa-solid fa-trash" style="color: #ffffff;"></i>
              <% end %>
            </div>
            <div class="col-12">
              <%= plan_detail.text_area :body, class: "form-control my-3", placeholder: "詳細説明" %>
            </div>
            <%= plan_detail.hidden_field :_destroy, class: 'destroy-field' %>
          </div>
        <% end %>

        <div class="row">
          <div class="col text-center">
            <div id="plan_details_wrapper"></div>
            <%= link_to '+', '#', id: 'add_plan_details', class: "btn btn-outline-dark rounded-circle ml-auto shadow" %>
          </div>
        </div>

        <div class="row">
          <div class="col text-center my-3">
            <%= f.submit "変更", class: "btn btn-warning mx-auto" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>