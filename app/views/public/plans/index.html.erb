<!--タブメニュー-->
<div class="wrap">
  <ul id="tab-menu">
    <li><a href="#tab1" class="active">Map</a></li>
    <li><a href="#tab2" class="">すべて</a></li>
    <li><a href="#tab3" class="">フォロー中</a></li>
  </ul>

  <div id="tab-contents">
    <div id="tab1" class="tab">
      <!--Mapを表示-->
      <%= render 'layouts/search', object: search_path %>
      <%= render 'map_index', plan_details: @plan_details %>
    </div>

    <div id="tab2" class="tab">
      <!--プラン一覧を表示-->
      <%= render 'layouts/search', object: search_path %>
      <%= render 'index', plans: @plans, tags: @tags %>
    </div>

    <div id="tab3" class="tab">
      <!--フォローユーザープラン一覧を表示-->
      <%= render 'layouts/search', object: search_path %>
      <%= render 'index', plans: @followed_plans, tags: @tags %>
  </div>

  </div>
</div>