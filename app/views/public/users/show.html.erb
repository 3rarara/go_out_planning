<h1>ユーザーページ</h1>
<%= image_tag @user.get_profile_image(100,100) %>
<%= @user.name %>

<h1>投稿一覧</h1>

<% @plans.each do |plan| %>

  <%= link_to plan.title, plan_path(plan) %>
  <%= plan.body %>

<% end %>
